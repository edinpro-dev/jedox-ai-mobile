import React from "react";
import { Text, View } from "react-native";

interface TableProps {
    tableHead: string[];
}

const Table = () => {
    return (
        <View>
            <Text>Table</Text>
        </View>
    );
};

export default Table;
